"""TraceAudit — counterfactual auditing of agentic RAG.

The package root deliberately re-exports nothing. Import from the leaf module
you need (e.g. `from traceaudit.trace.schema import Trace`). A curated
top-level API may be added later only by deliberate decision, not by accident.
"""

__all__: list[str] = []
